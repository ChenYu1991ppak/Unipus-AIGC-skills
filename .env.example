# 凭证读取优先级（先到先得）：
#   1. 环境变量 UNIPUS_AIGC_TOKEN
#   2. ./.env                        ← 本文件复制成的那个，仅仓库内开发时用
#   3. ~/.config/unipus-aigc/.env    ← 装成 plugin 后的常态位置
#
# 推荐做法：在 Claude Code 里运行 /unipus-aigc:guide，把 JWT 告诉它，
# 由它调 `run.sh set-token --stdin` 写到第 3 处（权限 600，不回显 token）。
#
# 手动配置（仅仓库内开发）：
#   cp .env.example .env，然后填入下面的值。
#
# JWT 获取方式：登录 https://ai.unipus.cn → 开发者工具 → Local Storage
#               → userInfo 里的 jwt 字段。
#
# ⚠️ JWT 等于账号密码：不要提交到仓库、不要转发给任何人。
#    .env 已在 .gitignore 里。
#
# 业务 API 主机有两个，都指向同一个后端、用同一个账号，**不是二选一**：
#   https://uaigc.unipus.cn   逆向来源，config.API_BASE 的默认值（老链路用它）
#   https://aigc.unipus.cn    接口文档里写的域名（照文档实现新功能时用）
# 两个都不能写成 ai.unipus.cn——那只是前端静态资源站。
# 要换域名就在代码里显式覆盖 config.API_BASE，**不要改默认值**。

UNIPUS_AIGC_TOKEN=

# 可留空，客户端会自动从 JWT 的 openId claim 解出
UNIPUS_AIGC_OPEN_ID=
